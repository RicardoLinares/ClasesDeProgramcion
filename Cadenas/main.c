#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <stdio_ext.h>
int main()
{// strlen, strcpy, strcmp;

    // Tarea pasar el nombre y apellido a mayuscula;

    //cantidad = strcmp(cadena, "perrO");

    //strcpy(cadena, "HAHAHAHA");
    //cantidad = strlen(cadena);

    //printf("Ingrese texto: ");
    //scanf("%s", cadena);
    //fgets(cadena,20,stdin);
    //__fpurge(stdin);

    printf("%d", cantidad);
    return 0;
}
